// import { Socket } from 'socket.io';

// const buttonHandler = (io: unknown, socket: Socket) => {
//   const buttonClicked = (msg: string) => {
//     socket.broadcast.emit('new-button-click', msg);
//   };

//   socket.on('button-click', buttonClicked);
// };

// export default buttonHandler;
